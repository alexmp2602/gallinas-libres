<section
  id="nosotros"
  aria-labelledby="mision-title"
  class="section bg-orange-50 overflow-hidden scroll-mt-12 cv-auto contain-layout"
>
  <div id="misionContent" class="container text-center space-y-6 fade-slide-up">
    <h2 id="mision-title" class="section-title text-orange-800">
      Nuestra misión
    </h2>
    <p
      class="max-w-2xl mx-auto text-base sm:text-lg text-neutral-800 leading-relaxed"
    >
      Nuestra misión es promover el bienestar de nuestras gallinas, respetando
      su libertad y su naturaleza, mientras abastecemos a comercios y mayoristas
      con huevos frescos de alta calidad. Creemos que una alimentación sana
      comienza con un cuidado responsable, ofreciendo productos que cuidan el
      planeta, a los animales y a quienes confían en nosotros para su negocio.
    </p>
  </div>

  <script is:inline>
    (() => {
      const el = document.getElementById("misionContent");
      if (!el) return;

      const show = () => el.classList.add("show");

      if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
        show();
        return;
      }

      const rect = el.getBoundingClientRect();
      if (rect.top < innerHeight && rect.bottom > 0) {
        show();
        return;
      }

      const io = new IntersectionObserver(
        (entries) => {
          if (entries.some((e) => e.isIntersecting)) {
            show();
            io.disconnect();
          }
        },
        { root: null, rootMargin: "0px 0px -10% 0px", threshold: 0.01 }
      );
      io.observe(el);
    })();
  </script>
</section>
